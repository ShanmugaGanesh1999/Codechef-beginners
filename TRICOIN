#include <stdio.h>
int main(void) {
 int n,i,j,cnt,ctn;
 scanf("%d",&ctn);
 for(j=0;j<ctn;j++){  
     cnt=0;
     scanf("%d",&n);
 for(i=1;i<=n;i++){
     if(n>cnt){
         n-=i;
         cnt++;
     }
 }
 printf("%d\n",cnt);
}
     
 }
