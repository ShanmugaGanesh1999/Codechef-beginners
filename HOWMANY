#include <stdio.h>
#include <string.h>

int main(void) {
	int n;
	scanf("%d",&n);
	char res[20];
	sprintf(res, "%d",n);
	int l;
	l = strlen(res);
	if(l<4) 
	{
	    printf("%d",l);
	}
	else
	{
	    printf("More than 3 digits");
	}
	
	
	return 0;
}

