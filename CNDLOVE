#include<stdio.h>
main(){
	int t,i,n,j,sum,k;
	scanf("%d",&t);
	for(i=1;i<=t;i++){
		sum=0;
		scanf("%d",&n);
		for(j=1;j<=n;j++){
			scanf("%d",&k);
			sum+=k;
		}
		if(sum%2!=0)
		  printf("YES\n");
		else
		  printf("NO\n");
	}
}
