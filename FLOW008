#include <stdio.h>
#include <math.h>
int main(void) {
    int n,i;
    scanf("%d",&n);
    while(n--){
        scanf("%d",&i);
        if (i<10)
            printf("What an obedient servant you are!\n");
        else
        printf("-1\n");
    }
	return 0;
}

