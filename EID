for _ in range(int(input())):
    n=int(input())
    v=list(map(int,input().split()))
    mini=10**6
    v.sort()
    for i in range(1,n):
        if(v[i]-v[i-1]<mini):
            mini=v[i]-v[i-1]
    print(mini)
