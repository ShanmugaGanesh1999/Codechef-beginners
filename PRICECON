# cook your dish here
for _ in range(int(input())):
    n, k = map(int, input().split())
    a = [int(x) for x in input().split()]
    ans = 0
    for item in a:
        if item > k:
            ans += item - k
    print(ans)
