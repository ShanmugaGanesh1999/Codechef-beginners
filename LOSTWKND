#include <stdio.h>

int main(void) {
	int t;
	scanf("%d",&t);
	while(t--)
	{
	    int sum=0;
	    for(int i=0;i<5;i++)
	    {
	        int a;
	        scanf("%d",&a);
	        sum+=a;
	    }
	    int p;
	    scanf("%d",&p);
	    sum*=p;
	    sum = sum - 120;
	    if(sum > 0)
    	    printf("Yes\n");
        else
            printf("No\n");
	}
	return 0;
}

