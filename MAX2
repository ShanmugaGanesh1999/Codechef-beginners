#include<stdio.h>
int main()
{
    int n,i,c=0;
    scanf("%d",&n);
        char s[n];
    scanf("%s",s);

    for(i=n-1;i>=0;i--)
    {
        if(s[i]=='0')
        {
            c++;
        }else
        {
            break;
        }

    }
    printf("%d\n",c);
    return 0;
}

