#include <stdio.h>

int main() {
	int i,n,sum=0,rem;long long t;
	scanf("%d",&n);
	for(i=0;i<n;i++){
	    scanf("%ld",&t);
	    while(t!=0){
	        rem=t%10;
	        sum+=rem;
	        t/=10;
	    }printf("%d\n",sum);
	    sum=0;
	}
    return 0;
}
